/* const From = {
  [Symbol(String)]: string => string.split(',').map(parseFloat),
};

for (const type of From) {
  console.log(type);
}*/

export default class BBOX {
  constructor(...args) {
    // const points = [...args];
    console.log(args);
  }
}
